FROM node:22-alpine

WORKDIR /app

# 依存関係を先にインストールしてキャッシュを効かせる
COPY package.json package-lock.json* ./
RUN if [ -f package-lock.json ]; then npm ci; else npm install; fi

EXPOSE 5173

CMD ["npm", "run", "dev"]
